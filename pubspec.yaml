name: flutter_live_market_watchlist
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_bloc: ^9.1.0
  equatable: ^2.0.5

  # DI
  get_it: ^8.0.3
  injectable: ^2.5.0

  # Networking / SSE transport
  dio: ^5.7.0
  flutter_test:
    sdk: flutter
  fake_async: ^1.3.3
  bloc_test: ^10.0.0
  bloc: ^9.2.1

dev_dependencies:
  flutter_lints: ^6.0.0

  # Codegen for DI
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2

  # Testing
  mocktail: ^1.0.4
flutter:
  uses-material-design: true
